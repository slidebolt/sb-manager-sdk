// Package managersdk provides the LocalStack integration test harness
// that wires all plugins together in-process. The core test primitives
// (TestEnv, MessageSpy) live in sb-testkit; this package adds the
// full-stack orchestration that depends on every plugin.
package managersdk
